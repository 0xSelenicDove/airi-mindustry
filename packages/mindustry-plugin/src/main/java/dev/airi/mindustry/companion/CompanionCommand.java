package dev.airi.mindustry.companion;

/** Explicit, player-visible operation requested of the deterministic companion core. */
public enum CompanionCommand {
    ANSWER_EXPOSURE,
    SUGGEST_BLUEPRINT,
    PREFLIGHT_BLUEPRINT,
    CONFIRM_BLUEPRINT,
    REPEAT_ALERT,
    ASSIGN_POLY,
    PROPOSE_REPAIR,
    CONFIRM_REPAIR,
    OBSERVE_REPAIR,
    RESUME_REPAIR
}
